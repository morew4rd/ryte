const window = @import("library/window.zig");
const input = @import("library/input.zig");
const audio = @import("library/audio.zig");
const image = @import("library/image.zig");
const canvas = @import("library/canvas.zig");
const font = @import("library/font.zig");
const fs = @import("library/fs.zig");
const shapes = @import("library/shapes.zig");

pub usingnamespace window;
pub usingnamespace input;
pub usingnamespace audio;
pub usingnamespace image;
pub usingnamespace canvas;
pub usingnamespace font;
pub usingnamespace fs;
pub usingnamespace shapes;
